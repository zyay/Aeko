<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug only: user-entered LAN llama-server / noVNC. Release is HTTPS + loopback. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
